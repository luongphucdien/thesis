export enum ModeType {
    Move = "Move",
    Save = "Save",
    Floor = "Add Floor",
    Delete = "Delete",
    AR = "AR",
    Default = ModeType.Move,
}

export enum FurnitureType {
    Table = "table",
}
