export enum ModeType {
    Move = "Move",
    Save = "Save",
    Floor = "Add Floor",
    Delete = "Delete",
    AR = "AR",
    Bound = "Boundary Box",
    Default = ModeType.Move,
}

export enum FurnitureType {
    Table = "table",
}
